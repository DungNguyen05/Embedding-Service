# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false

# Model Configuration
# Options:
# - keepitreal/vietnamese-sbert (Best for Vietnamese, requires ~2GB RAM)
# - sentence-transformers/LaBSE (Good multilingual, requires ~1.5GB RAM) 
# - sentence-transformers/paraphrase-multilingual-mpnet-base-v2 (Good performance, requires ~1GB RAM)
# - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (Lighter, requires ~500MB RAM)
EMBEDDING_MODEL_NAME=keepitreal/vietnamese-sbert

# Device Configuration (auto, cuda, cpu)
DEVICE=auto

# Logging
LOG_LEVEL=INFO